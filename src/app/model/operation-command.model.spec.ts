import { OperationCommand } from './operation-command.model';

describe('OperationCommand', () => {
  it('should create an instance', () => {
    expect(new OperationCommand()).toBeTruthy();
  });
});
